CREATE TABLE users(
    id INT NOT NULL,
    name VARCHAR(25),
    age INT NOT NULL,
    birthday DATE NOT NULL,
    PRIMARY KEY(id)
);

CREATE TABLE news(
    id INT NOT NULL,
    headline VARCHAR(100) NOT NULL,
    creation DATE NOT NULL,
    text VARCHAR(200) NOT NULL,
    journalist
)